# FEUC website – Vite + React (Cloudflare Pages)

## Desarrollo local
- `npm install`
- `npm run dev` para el servidor local
- `npm run lint` para comprobaciones
- `npm run build` para el artefacto de producción (`dist`)
- `npm run preview` para revisar el build localmente
